> Note: This document is kept for historical context. For current algorithm coverage and gaps, see `docs/ALGORITHM_MATRIX.md`.

# Implementation Priority List (Archived)

Current priorities should be derived from the matrix and the flattened table:

- `docs/ALGORITHM_MATRIX.md`
- `docs/ALGORITHM_TABLE.md`

If you need a focused shortlist, base it on the `auroraml = no` rows in those files.
